

Install -> Choose Python PackAge lower left corner

Search and install
```
kagglehub   - kagglehub chính thức của Kaggle, dùng để truy cập tài nguyên (datasets, models, notebook outputs) một cách dễ dàng từ Python code

pandas  -Lam viec vs csv excel ....

seaborn   - seaborn là thư viện vẽ biểu đồ thống kê dựa trên matplotlib và tương thích tốt với pandas.

python-dotenv  -  python-dotenv là thư viện Python giúp bạn đọc các biến môi trường (environment variables) từ file .env và nạp chúng vào môi trường thực thi của Python.

langchain-community -langchain-community chứa các loader, tool, chat model, embeddings, và retriever do cộng đồng đóng góp. Ví dụ:


langchain-openai -  langchain-openai là một gói mở rộng chính thức của LangChain để tích hợp với API của OpenAI, đặc biệt là các mô hình như GPT-4, GPT-3.5, embedding, và các tools khác như function calling.

langchain-chroma là gói tích hợp chính thức giữa LangChain và ChromaDB, giúp bạn dễ dàng dùng Chroma làm vector store (cơ sở dữ liệu vector) trong hệ thống AI như chatbot, tìm kiếm ngữ nghĩa, Q&A tài liệu...

transformers -  transformers là thư viện mã nguồn mở của Hugging Face dùng để làm việc với các mô hình ngôn ngữ lớn (LLMs) 


gradio -  gradio là một thư viện Python mã nguồn mở giúp bạn dễ dàng tạo giao diện người dùng (UI) cho mô hình machine learning hoặc bất kỳ hàm Python nào chỉ với vài dòng code.

notebook - notebook là tên gói Python cài đặt Jupyter Notebook, một môi trường giúp bạn viết và chạy mã tương tác ngay trong trình duyệt. Nó hỗ trợ cả mã lệnh, văn bản, hình ảnh, biểu đồ và thậm chí cả HTML.

ipywidgets (tên đầy đủ: IPython Widgets) là một thư viện Python dùng để tạo các thành phần tương tác (UI widgets) trong môi trường Jupyter Notebook, JupyterLab hoặc Google Colab.
```



```

pip install -U langchain-huggingface

```

